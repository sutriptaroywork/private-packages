import { describe, expect, beforeAll, it } from '@jest/globals';

describe('AWS Secret Manager', () => {
  // beforeAll(async () => {
  //   connection = new Connection(ALL_CONNECTION);
  // });

  // afterEach(async () => {

  // });
  // afterAll(async () => {

  // });

  describe('get secrets', () => {
    it('should create an instance of AWS Secret Manager', async () => {
      // connection.ready();
    });
  });
});
